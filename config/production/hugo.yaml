######################## production configuration ####################
# The base URL of your site (required). This will be prepended to all relative URLs.
baseURL: https://ccs-amsterdam.github.io/ccsamsterdam.github.io/
googleAnalytics:
staticDir: static
enableRobotsTXT: true
build:
  writeStats: true
markup:
  goldmark:
    renderer:
      unsafe: true
